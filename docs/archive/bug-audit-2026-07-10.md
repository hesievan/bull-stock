# 项目 Bug 审计报告

- **审计日期**: 2026-07-10
- **审计范围**: `/Users/hesi/bull-market-heat-index` 全仓（src/、scripts/、.github/workflows/、Makefile、README/使用指南）
- **审计方法**: 客观信号（pytest / compileall）+ AST 导入可达性分析 + 数据库 schema 与全部 SQL 的表/列交叉校验 + workflow 脚本引用完整性检查

---

## 一、客观信号（基线健康度）

| 检查项 | 结果 |
|--------|------|
| `python -m pytest tests/ -q` | **70 passed** |
| `python -m compileall src scripts` | **clean**（无语法错误） |
| 导入可达性（AST 全仓扫描） | 仅 1 处真实 intra-project 导入错误 |
| 数据库表存在性（引用 vs schema） | 无缺失表（qvix_daily / _tmp_upsert 为运行时动态创建，已核实） |
| INSERT/UPDATE 列名匹配 | 无真实不匹配（stock_daily 的 high/low/close/amount 为解析器误报，已核实列存在） |

> 结论：核心 V2 引擎与 CI 主链路（run_daily → backfill_precompute → 种子库）在导入与 schema 层面是健康的。问题集中在**被 48b06e6 清理误删的脚本**以及**两个非 CI 脚本的残留引用**。

---

## 二、已修复的问题（本次会话）

| # | 问题 | 状态 | 提交 |
|---|------|------|------|
| F1 | `rebuild_seed.yml` / `daily.yml` 调用已删除的 `scripts/fetch_tushare_history.py` → CI `exit code 2` | ✅ 已重建并推送 | `37c9523` |
| F2 | `fetch_hist_constituents.py` 缺失 → `index_daily_pe` 全空 → V2 估值维度（PE/ERP/巴菲特）失效 | ✅ 已重建并推送 | `dc4f8f0` |
| F3 | `fetch_gdp.py` 缺失 → `gdp_quarterly` 空 → V2 巴菲特指标无 GDP 分母 | ✅ 已重建并推送 | `11cf42b` |

---

## 三、仍存在的 Bug / 缺口

### 🔴 P1 — CI 数据缺口（不报错但降级种子库）

| # | 文件/步骤 | 影响 | 状态 |
|---|-----------|------|------|
| B1 | `scripts/fix_turnover.py` 缺失（`rebuild_seed.yml` 第 55 行，已 `\|\| echo skipped`） | 换手率修正缺失 | ✅ **已修复** (`cea6e4a`) — 重建脚本，修正 `daily_turnover` 为 `SUM(amount)/SUM(circ_mv)*10` |
| B2 | `scripts/fetch_qvix.py` 缺失 | 低 | 已覆盖（无需修复）— QVIX 由 `backfill_precompute.py:_backfill_qvix_batch` 提供 |
| B3 | `scripts/backfill_bond_yield.py` 缺失 | 低 | 已覆盖（无需修复）— 国债收益率由 `fetch_tushare_history.py` 第 5 步提供 |

### 🟡 P2 — 非 CI 脚本的导入错误（运行中会崩溃）

| # | 位置 | 错误 | 状态 |
|---|------|------|------|
| B4 | `scripts/update_sectors.py:28` | `from src.indicators.calculator import calculate_sector_heat` → ImportError | ✅ **已修复** (`d5a1714`) — 改为 `from src.indicators.sector_calculator import ...` |

### 🟡 P2 — 非 CI 脚本的 SQL 表引用错误（遗留 V1 引擎）

| # | 位置 | 错误 | 状态 |
|---|------|------|------|
| B5 | `src/indicators/calculator.py:154` | 查询表 `index_constituents`（不存在） → OperationalError | ✅ **已修复** (`e2fd7da`) — 改为 `index_constituents_hist` |

### 🟢 P3 — 文档/引用瑕疵（不影响运行）

| # | 位置 | 说明 | 状态 |
|---|------|------|------|
| B6 | `Makefile` 引用 `scripts/data_manager.py` | 脚本已删，仅文档过期 | ✅ **已修复** (`c7adccd`) — `make status`→`db_maintenance.py`，`make backfill`→`fetch_tushare_history.py` |

### 🟢 P3 — Workflow 逻辑异味（非 bug，观察项）

| # | 位置 | 说明 | 状态 |
|---|------|------|------|
| B7 | `daily.yml` seed 上传条件 `contains(github.run_id, '0')` | 节流不可靠但无害 | ✅ **已修复** (`127a893`) — 改为 `github.run_number % 10 == 0`（手动 dispatch 始终上传） |

---

## 四、未覆盖项（声明）

- **前端 `web/`**：纯静态 JS（ECharts SPA），无 JS 单元测试/类型检查，本次未做运行时 JS 错误审计。
- **网络依赖路径**：tushare / akshare / optbbs 实际抓取需 token + 网络，无法在无网环境端到端验证（仅验证导入可达性与 schema 契约）。
- **三个 explore 子代理超时/报错**，改为本地确定性 AST + 正则审计替代，结论已交叉验证。

---

## 五、建议优先级

1. **B1（fix_turnover）** — 若换手率指标对业务重要，优先重建（V2 的 `daily_turnover` 计算逻辑见 `backfill_precompute.py:_compute_daily_turnover`，修复脚本应承接该口径）。
2. **B4（update_sectors 导入）** — 一行修复，低成本高确定性，建议直接改。
3. **B5（calculator index_constituents）** — 仅影响遗留 V1 路径；若 V1 不再使用可忽略，否则补表/改引用。
4. **B6 / B7** — 文档与节流异味，低优先级。
