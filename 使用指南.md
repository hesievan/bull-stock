# A股牛市热度指数 — 使用指南

> 版本: v3.30 | 更新: 2026-09-01

---

## 目录

- [快速开始](#快速开始)
- [每日运行](#每日运行)
- [指标体系](#指标体系)
- [市态标签](#市态标签)
- [指数牛市见顶预判](#指数牛市见顶预判)
- [重点行业监控](#重点行业监控)
- [结果文件说明](#结果文件说明)
- [前端页面](#前端页面)
- [飞书通知](#飞书通知)
- [数据维护](#数据维护)
- [常见问题](#常见问题)
- [项目结构](#项目结构)

---

## 快速开始

### 前置条件

- Python 3.12（依赖由 uv.lock 锁定, 见 pyproject.toml）
- tushare 账号（2000+ 积分）
- SQLite（系统自带）

### 安装

```bash
git clone <repo_url>
cd bull-market-heat-index
# 安装 uv (https://docs.astral.sh/uv/): curl -LsSf https://astral.sh/uv/install.sh | sh
uv sync          # 创建 .venv 并按 uv.lock 安装锁定依赖(生产+开发)
source .venv/bin/activate
cp .env.example .env
# 编辑 .env，填入你的 TUSHARE_TOKEN
```

### 验证

```bash
python -m pytest tests/ -v
python scripts/run_daily.py
```

---

## 每日运行

### 运行命令

```bash
# 计算今日
python scripts/run_daily.py

# 计算指定日期
python scripts/run_daily.py 2026-08-11
```

### 预计算表历史回填

首次搭建或切换成分股范围后，需要回填历史预计算表：

```bash
# 回填所有预计算表（含 seal_rate，纯本地计算无需 API）
python scripts/backfill_precompute.py

# 仅回填涨停封板率（独立脚本，支持断点续传）
python scripts/backfill_seal_rate.py

# 回填 16 指标历史趋势（indicator_history.json）
python scripts/backfill_indicator_history.py

# 强制重新计算指定表
python scripts/backfill_precompute.py --force --tables index_daily_pe
```

回填脚本会自动跳过已 up-to-date 的表，仅回填缺失或过期的数据。
涨停封板率为本地计算（从 `stock_daily` OHLC 自连接前日收盘价 → 涨停价 → 封板率），无需 tushare API 调用。

### CI 种子数据库

GitHub Actions 每日流水线使用 `seed-db` Release 中的压缩数据库作为初始数据：

```bash
# 本地构建种子库（回填完成后）
python scripts/backfill_precompute.py

# 压缩并上传
python scripts/db_tools.py compress
gh release upload seed-db data/heat_index.db.gz --clobber
```

CI 自动下载该 Release 附件，解压后增量更新当日数据。

### 输出示例

```
2026-09-01 12:20:00,000 [INFO] RUN SUMMARY: 26 OK / 0 FAILED / 2 SKIPPED (22.6s)
```

所有 Step 独立 try/except，单个 Step 失败不阻断后续流程。

### 流水线步骤

| Step | 名称 | 说明 | 耗时参考 |
|------|------|------|---------|
| S0 | init_db | 数据库建表/迁移（schema v14） | <0.1s |
| S1 | S1_index | 7指数日行情增量拉取（沪深300/创业板/科创50/北证50/A500/中证1000/中证红利） | 1–5s |
| S2 | S2_market | 全市场日K线 + PE/PB/市值 | 3–8s |
| S25 | S25_index_pe | 成分股PE/PB中位数 | 0.5–2s |
| S26b | S26b_total_mv | 全市场总市值(供巴菲特指标) | 0.5–2s |
| S26 | S26_circ_mv | 全市场流通市值(供融资余额比) | 0.5–2s |
| S27–S30 | updown/limit/below_net/ma | 预计算表（不计分） | 各 0.5–3s |
| S30b | S30b_new_high | 250日新高占比预计算表（供结构分） | 0.5–3s |
| S30c | S30c_turnover | 换手率预计算表（10年窗口，供情绪分） | 0.5–3s |
| S24 | precompute_check | 预计算表陈旧检测 | <0.1s |
| S24c | S24c_m2 | M2 月度数据（成交额M2比） | <0.3s |
| S24d | S24d_m1 | M1 月度数据（M1-M2 剪刀差） | <0.3s |
| S24f | S24f_south | 南向净买额（akshare 全量 upsert） | 1–3s |
| S24g | S24g_futures | IF 基差（akshare IF0 + 库内沪深300） | 1–3s |
| S24h | S24h_accounts | 月度新增开户数（展示不计分） | 0.5–2s |
| S24i | S24i_etf_flow | 宽基 ETF 份额日快照（展示不计分） | 1–5s |
| S31 | S31_qvix | CFFEX股指期权恐慌指数（optbbs） | 1–5s |
| S3 | margin/bond_yield | 融资融券 + 国债收益率（akshare） | 0.1–0.5s |
| S35 | **S3_shenwan** | **申万行业分类（首次）/ 跳过** | **1–5s** |
| S36 | **S3_industry** | **证监会行业分类（首次，供板块热度）/ 跳过** | **1–5s** |
| **S5** | **S5_calc** | **V2引擎：16指标 + 加权合成 + 市态标签** | **5–15s** |
| **S55** | **S55_index_heat** | **七大指数牛市见顶预判** | **<0.1s** |
| S6 | save | 保存JSON（含低频锚展示键） | <0.1s |
| S7 | sectors | 板块热度（CSRC） | 1–7s |
| **S75** | **S75_focus** | **重点行业热度（申万一级10大行业，指数行情并行拉取）** | **0.5–1s** |
| S8 | final_save | 最终保存 | <0.1s |
| S9 | notify | 飞书/Bark推送 | <0.5s |

---

## 指标体系

### V2 引擎（16 指标 / 4 维度 — 每日流水线所用）

> 权重与参数由 `config/dev.yaml` / `config/prod.yaml` 的 `v2_engine` 段驱动（YAML 为唯一事实源）。
> 完整数据表清单、各指标牛熊区分度与回测验证见 [`README.md`](README.md)。

| 维度 | 权重 | 子指标 | 权重 | 说明 | 方向 |
|------|------|-------|------|------|------|
| **估值** | 28% | 大盘PE分位 | 14% | 全市场PE中位数滚动分位（含成分股数量过滤） | 越高越贵 |
| | | 巴菲特指标 | 14% | A股总市值 ÷ 年度GDP（GDP缺失年份自动降级取最近可用） | 越高越贵 |
| **资金** | 15% | 两融余额市值比 | 5% | (融资+融券)÷流通市值（85%分位起饱和收敛） | 越高越热 |
| | | 国债期限利差(10Y-2Y) | 3% | A股实证牛市期利差偏低（曲线走平），方向翻转（低利差=宽松=高分） | 低→高分 |
| | | M1-M2剪刀差 | 3% | M1同比 − M2同比（货币活化程度） | 越高越热 |
| | | 南向净买额 | 1% | 南向通当日成交净买额（跨境资金活跃度） | 越高越热 |
| | | 融资买入占比 | 3% | 融资买入额 ÷ 两市成交额（杠杆激进程度） | 越高越热 |
| **情绪** | 35% | 涨停封板率 | 6% | 收盘涨停数÷盘中触板数（本地计算，stock_daily OHLC 自连接前日收盘价） | 越高越热 |
| | | 成交额M2比 | 14% | 日成交额 ÷ M2 | 越高越热 |
| | | 换手率 | 9% | 成交额÷流通市值×10（10年窗口，daily_turnover预计算表） | 越高越热 |
| | | IF基差率 | 2% | (IF主力−沪深300)÷现货（升水=机构乐观） | 越高越热 |
| | | 振幅热度 | 2% | 沪深300日内(高−低)÷昨收（多空博弈激烈度） | 越高越热 |
| | | 已实现波动率 | 2% | 沪深300 20日对数收益std×√250（**替代 iVIX 期权隐波**） | 低→高分 |
| **结构** | 22% | 创新高占比 | 12% | 250日新高股票占比（含顶背离惩罚） | 越高越好 |
| | | MA排列比 | 6% | MA20>MA60>MA120占比 | 越高越好 |
| | | 涨跌家数广度 | 4% | 上涨家数÷下跌家数（普涨/普跌宽度） | 越高越好 |

> **权重调整史**：
> - **2026-08-12**：涨停封板率区分度仅 3.1（旧体系最低）、单日触板样本小时比率极不稳定，由 15% 降至 8%；释放的 7% 补偿给创新高占比(10%→14%)、MA 排列比(5%→8%)。**北向净流入比退役**（港交所 2024-08-19 起停止披露北向净买入额，tushare `moneyflow_hsgt` 此后返回失真膨胀量级，且 CI 抓取长期不稳定），其 4% 权重重配给两融余额市值比(5%)、国债期限利差(3%)、M1-M2 剪刀差(3%)。
> - **P1-P3（2026-09）**：新增南向净买额(1%)、IF 基差率(2%)、涨跌家数广度(4%)、融资买入占比(3%)、振幅热度(2%)、已实现波动率(2%)六指标；封板率 8%→6%，创新高 14%→12%、MA 排列 8%→6%（权重补给广度）。最终四维度：估值 28% / 资金 15% / 情绪 35% / 结构 22% = 100%。回测门禁不退化（区分度 15.0 / 同期 0.816 / 领先 −0.230）。

**百分位口径**：所有指标统一使用**滚动窗口分位**（最近 1260 个交易日 ≈ 5 年），替代全历史分位以消除 regime drift（`v2_engine.percentile.rolling_window: 1260`，环境变量 `HEAT_PCT_WINDOW` 可覆盖；序列短于窗口时退化为全历史）。

**展示不计分（3 项）**:

1. **QVIX 恐慌指数**（含三品种成分 + 集中度，optbbs 源）
2. **新增投资者开户数**（月度，中国结算；2023-08 后源停更，附月份标签；前端低频锚展示）
3. **宽基 ETF 总份额**（16 只宽基 ETF 日快照，自 2026-09-01 起积累，历史 ≥180 日后评估入分；前端低频锚展示）

### 评分合成路径

```
原始数据 → 预计算表(口径统一) → 滚动窗口 1260 日百分位(0-100)
        → 指标按权重加权合成综合分 → 3日平滑 → 四区间分类 + 市态标签
```

### 背离惩罚

- **情绪背离**: 换手率分 >70 且指数跌幅 >1.5% → 情绪得分扣 20%（回看 20 日）
- **新高顶背离**: 指数涨 + 新高占比下降且 <30% → 结构分扣减最多 15 分

### 热度阈值

| 颜色 | 分数 | 含义 |
|------|------|------|
| 🟢 绿色 | 0–40 | 估值合理，情绪冷淡 |
| 🟡 黄色 | 40–55 | 部分指标偏高 |
| 🟠 橙色 | 55–65 | 多项指标偏高，考虑减仓 |
| 🔴 红色 | 65–100 | 多项指标历史高位，考虑离场 |

---

## 市态标签

引擎输出 `regime` 字段，按综合分与结构健康度划分市场状态，前端概览页以徽标展示：

| 市态 | 判定 | 含义 |
|------|------|------|
| 🔴 过热 | 综合分 ≥65 且结构分偏高 | 全面亢奋，减仓信号密集 |
| 🟡 分歧 | 综合分 40–64 | 多空胶着，结构性机会 |
| 🔵 修复 | 综合分 <40 且趋势转好 | 从冰点回暖，观察期 |
| 🟢 冰点 | 综合分 <40 且结构低迷 | 极度冷淡，历史底部区域 |

> 结构维度出现破位风险（创新高占比骤降 / MA 排列转空）时，徽标附加"破位风险"标记（红色）。

---

## 指数牛市见顶预判

### 目标指数

| 指数 | tushare代码 | akshare代码 | 数据起始 |
|------|------------|------------|---------|
| 沪深300 | 000300.SH | sh000300 | 2014-12-29 |
| 创业板指 | 399006.SZ | sz399006 | 2014-12-29 |
| 科创50 | 000688.SH | sh000688 | 2020-01-02 |
| 北证50 | 899050.BJ | bj899050 | 2022-12-19 |
| 中证A500 | 000510.SH | sh000510 | 2024-09-23 |
| 中证1000 | 000852.SH | sh000852 | 2014-12-29 |
| 中证红利 | 000922.SH | sh000922 | 2015-01-05 |

### 评分体系

每个指数独立计算:

**技术指标（4个子指标）**: MA250偏离度、20日动量、60日动量、120日动量

**估值指标（2个子指标）**: PE TTM 分位、PB 分位（仅沪深300、创业板指可用；中证红利无 ≥10 年 PE/PB 历史数据源，采用纯技术评分）

```
composite = tech_avg × 0.5 + val_avg × 0.5  # 有估值数据时
composite = tech_avg                           # 无估值数据时
```

> 沪深 300 额外展示市赚率 PR（PR = PE / ROE = PE² / PB，仅展示不参与评分）；量能评分仅作参考（tushare 量价数据单位不统一）。

### 信号阈值

与全市场热度一致：≥65 🔴 红 | ≥55 🟠 橙 | ≥40 🟡 黄 | <40 🟢 绿

### 使用建议

- **全市场热度为宏观择时参考**，指数热度用于判断具体板块风险
- 当全市场热度 ≥ 65（红区）且多个指数同时红区 → 系统性风险较高
- 创业板和科创50同时红区 → 成长股泡沫风险

---

## 重点行业监控

### 概述

系统持续追踪 **银行 / 非银金融 / 食品饮料 / 医药生物 / 电力设备 / 电子 / 有色金属 / 公用事业 / 房地产 / 煤炭** 十大申万一级重点行业，以及 **白酒 / 保险** 两个二级细分赛道的板块热度。每个行业输出：

- **申万行业指数行情**: 收盘价、涨跌幅（数据源: akshare `index_hist_sw`）
- **行业估值**: PE(TTM)、PB、静态股息率（数据源: akshare `sw_index_first_info`）
- **成分股热度**: 个股数、平均涨跌、涨跌比、估值/换手率/涨跌比三因子合成分
- **领涨/领跌**: Top 3 个股及其涨跌幅

### 评分逻辑

与全市场热度一致，采用**历史百分位法**：

```
行业热度分 = avg(PE中位数百分位, 换手率均值百分位, 上涨比例百分位)
```

- 百分位基准: 过去 365 个交易日
- 阈值: ≥70 🔴 热 / ≥40 🟡 温 / <40 🟢 冷

### 输出文件

`web/data/focus_industries.json` — 每日流水线 S75_focus 步骤自动更新。

### 前端查看

概览 Tab 底部新增 **重点行业热度 · 申万一级** 卡片区域，每个行业以独立卡片展示指数行情、估值、热度分和领涨领跌。

---

## 结果文件说明

所有计算结果输出到 `web/data/` 目录：

| 文件 | 说明 |
|------|------|
| `index.json` | 最新热度（综合 + 4维度 + 16指标原始值 + 市态标签 + 低频锚展示键） |
| `detail.json` | 含完整指标明细（16 指标百分位评分 + 原始值 + 恐慌指数成分分解） |
| `history.json` | 历史热度序列（周频，2015~今，与顶部卡片口径一致） |
| `indicator_history.json` | 16指标历史趋势（供前端图表） |
| `index_heat.json` | 七大指数最新过热评分 |
| `index_heat_history.json` | 七大指数历史评分时序 |
| `sectors.json` | 板块热度排名（CSRC行业分类） |
| `focus_industries.json` | **重点行业热度（申万一级10大行业 + 二级细分白酒/保险，含指数行情+PE/PB+领涨领跌）** |
| `run_status.json` | 各Step执行状态 |

### index.json 格式

```json
{
  "trade_date": "2026-08-11",
  "composite_score": 61.7,
  "composite_score_smoothed": 63.8,
  "level": "orange",
  "level_smoothed": "orange",
  "regime": {
    "label": "分歧",
    "structure_break_risk": false
  },
  "dimensions": {
    "valuation": { "score": 78.7, "label": "估值" },
    "fund":       { "score": 73.7, "label": "资金" },
    "sentiment":  { "score": 74.3, "label": "情绪" },
    "structure":  { "score": 11.6, "label": "结构" }
  },
  "indicators_v2": {
    "pe": 23.7339,
    "buffett": 0.9248,
    "margin_ratio_v2": 0.0266,
    "yield_spread": 0.4633,
    "m1_m2_spread": -4.0,
    "southbound": -13.7616,
    "margin_buy_ratio": 0.0953,
    "seal_rate": 0.7059,
    "turnover_m2": 0.0065,
    "turnover": 2.3304,
    "futures_discount": -0.0083,
    "amplitude": 0.0118,
    "realized_vol": 0.253,
    "new_high": 0.0108,
    "ma_alignment": 0.0526,
    "breadth": 0.4276
  },
  "display_new_accounts": 99.59,
  "display_new_accounts_month": "2023-08",
  "display_etf_shares": 2448.37,
  "qvix_display": null,
  "qvix_components": {
    "qvix_50": 18.97,
    "qvix_300": 21.19,
    "qvix_1000": null,
    "concentration": null
  },
  "version": "v2",
  "updated_at": "2026-08-11 00:00:00"
}
```

- `indicators_v2` 存储原始值（非百分位分数），由前端 `fmt` 函数按需格式化；共 **16 个键**（pe / buffett / margin_ratio_v2 / yield_spread / m1_m2_spread / southbound / margin_buy_ratio / seal_rate / turnover_m2 / turnover / futures_discount / amplitude / realized_vol / new_high / ma_alignment / breadth）
- `regime` 为市态标签（label / extreme / structure_break_risk；label 取值 过热≥65 / 分歧≥55 / 修复≥40 / 冰点<40，与展示色档同源同切点；extreme 为极值叠加信号 extreme_hot≥80 / extreme_cold≤29；破位风险为 true 时前端标红。M1.6 起阈值统一读 config heat_levels，单一事实源）
- `display_new_accounts` / `display_new_accounts_month` / `display_etf_shares` 为低频锚展示键（不计分；开户数为月度、ETF 为日快照）
- 历史百分位分数记录在 `indicator_history.json` 中供牛熊对比
- `qvix_display` / `qvix_components` 为恐慌指数展示字段（不计分）

---

## 前端页面

```bash
cd web && python3 -m http.server 8080
# 浏览器访问 http://127.0.0.1:8080/app.html
```

页面结构：
- **概览 Tab**: 综合仪表盘（仪表盘 + 市态徽标 + 低频锚行：新增开户 / 宽基ETF份额）+ 历史折线图 + **七大指数热度卡片**（含量能(参考)与市赚率 PR 展示）+ **重点行业热度卡片**（评分折线图，按红/黄/绿分段着色）
- **指标拆解 Tab**: **16 指标卡片**（当前值 + 牛熊均值 + 趋势图）+ 恐慌指数卡片（公式 + 三品种成分值 + 集中度）
- **历史 Tab**: 近30日运行记录（含维度分与状态）

辅助功能：
- **⤓ CSV 导出**：一键导出历史数据（含 BOM，Excel 可直接打开中文）
- **◐ 主题切换**：深/浅色切换，持久化到 localStorage
- **键盘导航**：Tab 间 ←/→ 切换（ARIA 无障碍）
- **失败重试**：数据加载失败显示错误清单与重试按钮

依赖: `echarts.custom.min.js`（按需定制构建 650KB，本地文件，无需网络加载）。

---

## 飞书通知

### 配置

在 `.env` 中添加：
```env
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxx
```

### 关闭通知

不配置 `FEISHU_WEBHOOK_URL` 即可跳过通知步骤（S9 自动 SKIPPED）。

---

## 数据维护

### 数据量

SQLite 数据库约 1.7GB（**29 张业务表**，schema v14：2026-08-12 清理 12 张 V1 遗留/死表后 25 张，P1/P3 新增 4 张），核心大表 `stock_daily` 约 1126 万行。完整表清单见 [`README.md`](README.md) 的「数据表（29 张）」一节。

### 维护命令

```bash
# 查看数据库统计
python scripts/db_tools.py status

# 压缩（回收空间）
python scripts/db_tools.py vacuum

# 清理过期备份（保留最近 5 个 gzip 备份 + 最近 3 个 .bak_* 临时备份）
python scripts/db_tools.py cleanup --keep 5
# 仅预览将要删除的文件，不实际删除
python scripts/db_tools.py cleanup --dry-run
# 额外删除早于 30 天的备份
python scripts/db_tools.py cleanup --keep 5 --days 30
```

### 核心表概览（V2 消费）

| 表名 | 行数 | 更新频率 | 数据源 |
|------|------|---------|--------|
| stock_daily | ~1126万 | 每日增量 | tushare daily + daily_basic |
| index_daily | ~2.3万 | 每日增量 | tushare index_daily |
| margin_history | ~2600 | 每日增量 | tushare margin |
| bond_yield | ~1.1万 | 每日增量 | akshare / tushare yc_cb |
| stock_market_cap | ~2800 | 每日 | 实时计算 |
| index_daily_pe | ~2800 | 每日 | 实时计算 |
| daily_circ_mv | ~2800 | 每日 | 实时计算 |
| northbound_history | ~2700 | 每日 | tushare moneyflow_hsgt（已修复 2024-08-19 单位跳变；指标已退役，仅保留历史参考） |
| m1_monthly / m2_monthly | ~222 | 按月 | tushare cn_m |
| gdp_quarterly | ~104 | 按季 | tushare cn_gdp |
| qvix_daily | ~1600 | 每日 | optbbs CSV |
| daily_seal_rate | ~2800 | 每日 | 本地计算（stock_daily OHLC 自连接前日收盘价 → 涨停价 → 封板率） |
| daily_turnover | ~2800 | 每日 | 实时计算（10年窗口） |
| daily_new_high | ~2800 | 每日 | 实时计算 |
| daily_ma_alignment | ~2800 | 每日 | 实时计算 |
| daily_hsgt_south | ~2700 | 每日 | akshare（南向净买额） |
| daily_futures_basis | ~2300 | 每日 | akshare IF0 + index_daily（IF 基差） |
| stock_shenwan | ~1600 | 首次拉取（按需更新） | akshare index_component_sw |
| monthly_accounts | 101 | 按月（2023-08 后源停更） | akshare stock_account_statistics_em |
| daily_etf_flow | 逐日积累（自 2026-09-01） | 每日 | akshare fund_etf_spot_em |

> 注：北向净流入比指标已于 2026-08 退役（港交所 2024-08-19 起停止披露北向净买入额，且 CI 抓取长期不稳定）。`northbound_history` 表仍由维护脚本保留历史数据，但不再参与热度计算，仅作参考。
> `monthly_accounts` / `daily_etf_flow` 为 P3 低频锚展示表（不计分）：开户数为月度低频锚（2023-08 后源停更，展示时附月份标签）；ETF 份额为日快照，自采集日起积累，待历史 ≥180 日后再评估入分。
> 已删除的 V1 遗留 / 死表（limit_up_daily、sector_heat、stock_balance、daily_erp、daily_macro、daily_ma250、daily_sector_div、macro_money、new_investors、heat_index、ah_premium、ah_premium_monthly）不再存在，`init_database` 不会重建。

---

## 常见问题

### Q: 运行报 `TUSHARE_TOKEN` 未设置

A: 确保 `.env` 文件存在且内容正确：
```
TUSHARE_TOKEN=你的token
```
系统还自动搜索 `~/daily_stock_analysis/.env` 作为备选。

### Q: 某个 Step 一直 FAILED

A: 检查网络连接和 tushare 积分余额：
```bash
cat web/data/run_status.json
```
常见原因：
- tushare 积分不足（需要 2000+）
- 网络超时（重试即可）
- 非交易日运行

### Q: 为什么某些指数没有 PE/PB 评分

A: tushare 的 `index_dailybasic` 仅覆盖沪深300和创业板指等少数指数。科创50/北证50/A500/中证1000/中证红利没有指数级 PE/PB 数据（中证红利亦无 ≥10 年历史数据源），使用纯技术评分。

### Q: 非交易日能运行吗

A: 可以，但部分需要日频数据的步骤会 SKIP。S5_calc 仍可计算（使用最新可用数据）。

### Q: 数据库太大怎么办

A: 运行 `python scripts/db_tools.py vacuum` 压缩。

### Q: 为什么涨停封板率权重只有 6%

A: 回测实测其牛熊区分度仅 3.3（16 指标中倒数第三），且单日触板样本小时比率极不稳定、单日可摆 ±60~95 分。经历 15%→8%（2026-08-12）→6%（P3）两次降权，释放权重补给结构维度（创新高占比 / MA 排列 / 涨跌家数广度），预测力未受损。

### Q: iVIX 指标为什么没有接入

A: roadmap 原列 iVIX（上证50ETF 期权隐含波动率），因免费数据源不稳定未接入；以**现货已实现波动率**（沪深300 20 日对数收益 std×√250）替代进分，同属"波动率低=贪婪"负向逻辑，数据源稳定且回测不退化。如需真 iVIX 可后续以 akshare `option_risk_indicator_sse` 补充（作展示性指标，不进分）。

### Q: 为什么前端显示"新增开户 99.6万户 (2023-08)"

A: 新增投资者开户数来自中国结算/东财源，该源自 2023-08 起停止更新，展示值为历史锚点（非实时），页面附月份标签以提示。此指标仅展示不入分。

### Q: 为什么宽基ETF份额只有一个点

A: ETF 份额历史无法免费回填，自 2026-09-01 起每日快照积累；待历史 ≥180 日后再评估是否入分（roadmap P3.5 设计）。

---

## 项目结构

```
bull-market-heat-index/
├── src/
│   ├── config.py                   # 配置加载（YAML 为唯一事实源，16 权重校验）
│   ├── data/
│   │   ├── database.py             # SQLite管理（29表，WAL模式，schema v14）
│   │   ├── fetcher.py              # 数据获取（tushare + akshare）
│   │   └── qvix_fetcher.py         # CFFEX 股指期权恐慌指数（optbbs CSV）
│   ├── indicators/
│   │   ├── heat_index_v2.py        # ⭐ V2引擎（16指标/4维度，滚动分位 + 市态标签，YAML 权重驱动）
│   │   ├── utils.py                # 共享工具函数
│   │   ├── index_heat.py           # 七大指数过热预判
│   │   ├── sector_calculator.py    # 板块热度（CSRC行业分类）
│   │   └── focus_industries.py     # ⭐ 重点行业热度（申万一级10大行业 + 二级细分白酒/保险，指数行情并行拉取）
│   └── output/
│       └── json_writer.py          # JSON输出+飞书通知
├── scripts/                        # 每日流水线入口 + 各工具/回测脚本
│   ├── run_daily.py                # 每日流水线入口（S0-S10）
│   ├── api_server.py               # REST API
│   ├── backtest_v2.py              # V2全历史回测（门禁验证）
│   ├── backfill_precompute.py      # V2预计算表历史回填
│   ├── backfill_seal_rate.py       # 涨停封板率历史回填（本地计算）
│   ├── backfill_indicator_history.py # 16指标历史回填
│   ├── regen_today_snapshot.py     # 当日快照重生成
│   ├── db_tools.py                 # 数据库综合工具（状态/压缩/备份/归档）
│   └── ...                         # 回测/分析工具
├── tests/                          # 116个单元测试
├── web/
│   ├── app.html                    # 前端SPA（16指标 + 市态徽标 + 低频锚）
│   ├── echarts.custom.min.js       # 定制 ECharts（650KB）
│   └── data/                       # JSON输出目录
├── config/                         # prod.yaml / dev.yaml（16 权重 + 滚动窗口）
├── reports/                        # 报告
├── pyproject.toml                   # 依赖声明源(单一来源)
└── uv.lock                          # 依赖锁定(多平台+sha256 哈希, 勿手改)
```

---

*文档版本: v3.30 | 更新时间: 2026-09-01 | 16 指标 / 4 维度体系 + 滚动窗口分位 + 市态标签 + 低频锚展示（P0-P3 全量落地）。测试 116 例全部通过。*
